TODO:

fix auth.php decoder to go simpler according to (https://base64.guru/developers/php/examples/base64url)

handle the variable sent for authentification, actually store them to database, set a cookis

javascript when the request is accepted

finish the login part

finish the logout

clean and round up aith authentification

push to githug

TODO RESEARCH:
    how to set the JWT token in a way that the browser will (automatically) use that in Authrozation header with 'Bearer' for the future requests

DONE: fix auth.php decoder to go simpler according to (https://base64.guru/developers/php/examples/base64url)

DONE: handle the variable sent for authentification, actually store them to database, set a cookie


check if the email adress is unique on signup

clean up the suthentification system before moving


RESEARCH:
    how to set the JWT token in a way that the browser will (automatically) use that in Authrozation header with 'Bearer' for the future requests

